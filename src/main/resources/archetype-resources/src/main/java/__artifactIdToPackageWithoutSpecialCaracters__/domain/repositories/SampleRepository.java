package ${groupId}.${artifactIdToPackageImport}.domain.repositories;

import ${groupId}.${artifactIdToPackageImport}.domain.entities.Sample;

public interface SampleRepository {

  Sample getSample(Integer id);

}
